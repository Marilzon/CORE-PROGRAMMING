#include <iostream>

using namespace std;

<<<<<<< HEAD
int main()
{
	/* Declaring a variable called `integer` and it is a `int` type. */
	int integer; // 1, 2, 3
	cout << "Input a number: ";
	cin >> integer;

	/* Declaring a variable called `character` and it is a `char` type. */
	char character; // "A"
	cout << "Input one character: ";
	cin >> character;

	/* Declaring a variable called `vetorOfCharacteres` and it is a `char` type. */
	char vetorOfCharacteres[20];
	cout << "input a characters: ";
	cin >> vetorOfCharacteres;

	/* Declaring a variable called `biggerValue` and it is a `double` type. */
	double biggerValue; // 2.4999999, bigger then float
	cout << "input a real number: ";
	cin >> biggerValue;

	/* Declaring a variable called `floatValue` and it is a `float` type. */
	float floatValue; // 2.49
	cout << "input a float value: ";
	cin >> floatValue;

	/* A boolean variable. */
	bool trueOrFalse = true; // true or false

	/* A string variable. */
	string phrase = "this is a string phrase: "; // "the string like a `line` of characteres"

	cout << integer << endl;
	cout << character << endl;
	cout << floatValue << endl;
	cout << trueOrFalse << endl;
	cout << vetorOfCharacteres << endl;
	cout << biggerValue << endl;
	cout << phrase << endl;

	// empty variables are initialized with a empty value.

	/* Returning a number 0, if all bellow commands are ok. */
	return 0;
=======
int main () {
	/* Declaring a variable called `integer` and it is a `int` type. */
	int integer; // 1, 2, 3

	/* Declaring a variable called `character` and it is a `char` type. */
	char character; // "A"

	/* Declaring a variable called `biggerValue` and it is a `double` type. */
	double biggerValue; // 2.4999999, bigger then float

	/* Declaring a variable called `floatValue` and it is a `float` type. */
	float floatValue; // 2.49

	/* A boolean variable. */
	bool trueOrFalse; // true or false

	/* A string variable. */
	string userName;
>>>>>>> 36a292607854e2bbef5af41559ee5cb7039f954c
}
